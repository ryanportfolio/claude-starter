---
description: One-time first-session setup for a project spawned from the claude-starter template — detects the stack, fills CLAUDE.md's FILL IN sections via Q&A, seeds reference files, tunes the best-practices catalog, commits.
---

Invoke the `init-project` skill via the Skill tool, passing through any provided arguments: $ARGUMENTS
