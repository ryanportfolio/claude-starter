---
description: End-to-end safe shipping workflow — audit working tree, branch off main if needed, stage only intended files, verify, commit, push, open PR. Use when the user says /safe-ship, asks to "ship these ch
---

Invoke the `safe-ship` skill via the Skill tool, passing through any provided arguments: $ARGUMENTS
