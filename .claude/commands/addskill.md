---
description: Add a new skill to this repo so it's available in every Claude Code web session. Use when the user says /addskill, asks to "add a skill", "install a skill", "create a skill", or wants a third-party sk
---

Invoke the `addskill` skill via the Skill tool, passing through any provided arguments: $ARGUMENTS
